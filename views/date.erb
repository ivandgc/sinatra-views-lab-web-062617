<!DOCTYPE html>
<html>
    <body>
        <h1>Today</h1>
        <p> <%= "The date is #{Time.now.strftime("%A")}, #{Time.now.strftime("%B")} #{Time.now.day}, #{Time.now.year}" %> </p>
    </body>
</html>
